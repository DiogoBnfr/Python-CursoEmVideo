# Crie a média entre duas notas de um aluno.
n1 = float(input('Primeira nota do aluno:'))
n2 = float(input('Segunda nota do aluno:'))
med = (float(n1) + float(n2)) / 2
print('A média entre {} e {} é de {}.'.format(n1, n2, med))
